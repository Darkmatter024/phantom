# CLAUDE.md — PHANTOM (darkmatter024/phantom)

## What this is
PHANTOM is an offline-first, single-file iOS Safari PWA (`dct-ios.html`, ~44k lines) for CoreWeave data-center technicians. Live at darkmatter024.github.io/phantom/dct-ios.html. Built and owned by John (Lead DCT). It is a FLEET-WIDE app: ships site-agnostic; the loaded master file provides all site-specific data (§ Design Law 6).

## Roles
- **John**: owner. All gate decisions. All device verifies (iPhone, real hardware). His word is final.
- **web-Claude**: authors `.md` handoff specs with verbatim `str_replace` anchors.
- **You (Claude Code)**: execute specs, git operations, verification. You may fix draft-spec bugs against live code (verify helper names, selectors, signatures before use) — report every deviation from spec in the commit message.

## Ship discipline — every ship, no exceptions
0. **BATCH VERIFY (CALL 0, DIRECTIVE 2026-07-06, owner-delegated):** ships may stack; device verification = one consolidated pass via  (repo root, Claude Code maintains). Cap: every 6 stacked ships or before any HIGH-risk (LR-2 class) ship. All other gates stay per-ship. Every ship note cites the CALL number it relies on; owner veto reverts the specific call.
1. **OODA first**: `curl` live `main` (`dct-ios.html`, `sw.js`, `version.json`) before ANY edit. If live version ≠ spec baseline, STOP and re-anchor. Verbatim strings are truth; line numbers are hints.
2. **Surgical edits only**: `str_replace` with unique anchors. No rewrites, no "while I'm here" changes, no drive-by refactors.
3. **Gates before push**: `node --check` ×3 · CSS brace-balance check · CRLF preserved.
4. **Three-stamp lockstep**: `dct-ios.html` + `sw.js` CACHE_VERSION + `version.json` bump together, always.
5. **One ship per version.** Never stack a second UI change on an unverified one.
6. **Device verify is a HARD STOP.** After push, hand John the verify checklist and PARK. Do not proceed to the next ship on your own initiative.
7. **`?legacy=1` is byte-identical-behavior.** Legacy markup, `showPage`/`showOpsTab`, `#ops-tab-strip`, and legacy render paths are untouchable except in a John-signed R1 deletion pass. "Hide, not delete; rip-cord restores it."

## Hard rules (violations found in the field — never reintroduce)
- **No silent failures.** Never `if (!x) return;` on a user-facing path. Fail loudly: `console.warn` + `phantomToast`. (Root cause of the dead Work-grid cards.)
- **No legacy page IDs in nav state under redesign.** Nothing running under `body.rd` may `nav_push` a legacy `p:` value (e.g. `p:'sop'`). Back-nav must never be able to resurrect a legacy surface. (Root cause of the legacy-leak bug.)
- **One door per feature.** Never wire the same surface to two hand-built entry points. New entry points call the ONE canonical `rd_open*` function.
- **No silent host fallbacks.** Renderers targeting a redesign host must not quietly fall back to a legacy container (`deploy_opsHost` pattern). Warn and abort instead.
- **Merge, never overwrite.** Master-file loads fill gaps in user data; hand-entered values always win and always survive reloads.
- **No base64 images in localStorage.** Photos/attachments: ephemeral or IndexedDB only.
- **Names say what the door opens.** No aspirational or historical labels (MASTER DOC→SITE PROFILE precedent).
- **Icon law (owner ruling 2026-07-05):** No large legible title words on icon art (they ghost against card labels); ambient numerals / micro-labels / readouts are approved texture and encouraged for realism. Owner exception of record: the MASTER FILE plaque text ("MASTER DOC / DEPLOYMENT AUTHORIZED").

## Design law (test every change against these)
1. Shift-shape, not history-shape: COMMAND = situational awareness · WORK = job chain in execution order (Deploy → Master → BOM → Manifest → Port Map → Rack Map → Scan → Handoff) · CRASH CART = zero-state troubleshooting bench.
2. Zero-state test: works with no deployment loaded → CRASH CART reference. Needs deploy state → lives inside the Deploy flow.
3. Additive over surgery: re-homed legacy organs (pg-power family) get new redesign-native layers routed into existing content. No internal rewrites before R4.
4. Cold Aisle Filter: every feature must help a gloved tech in the aisle right now. Tap depth matters. Four taps to reach a reference is a defect.
5. Aesthetic bar: "$10M, not cheesy." Tokens: `--bg:#04060a --cyan:#28e0ff --vio:#8a4bff --mag:#ff2bd6 --teal:#1fffd0 --gold:#ffcb45`. Fonts: Orbitron/Chakra Petch/Rajdhani. Minimum formatting, no clutter.
6. Site data flows from the master; the app carries only fleet truth. One master load hydrates Site Profile, Rack Map, Port Map. Profiles are per-facility; switching masters switches context.

## Current state & queue (update this section every ship)
- Live: `v1.14.226` — **PERSISTENT BUILD BADGE** (§10; running-version stamp top-right from `PHANTOM_APP_VERSION`, `body.rd`-gated, `pointer-events:none`). Caps a large session arc `.218`→`.226` off the `.217` base (2026-07-09/10):
  - **.218** 3D rack view (three.js r128, vendored/offline `vendor/three.min.js`, opt-in FLAT|3D on the rack-detail elevation, single-context `_reh3dActive` lifecycle) · **.219** 3D camera/scrubber fix · **.220** 3D render parity (cabinet enclosure + sRGB/ACES) · **.221** DEPLOY-BUG INSTRUMENT — reused existing `phantom_crash_log` (NOT a new store) + `phantom_logErr(ctx,err)` caught-error sink + un-silenced 12 deploy catches + SYS→ERRORS sheet + deploy fallback card (banner mutation `redesign_isOn()`-gated for legacy-strict) · **.222** FORGE 3D Stage 1 (cold-aisle scene ported VERBATIM from `forge-base (15).html`; orbit not FPS; ceiling/walls dropped; §2.8 placement F-01..F-18; `forge3d_*` single-context module w/ SYMMETRIC cross-dispose vs `_reh3dActive`; provisional door SYS→FORGE 3D) · **.223** DEPLOY DEAD-CARD FIX ✅**DEVICE-VERIFIED 2026-07-10** — root cause traced (4th attempt): deploy surfaces render into `#wk-deploy`, hidden by CSS `body.rd #pg-work.wk-grid .work-sub{display:none!important}`; silent success into a hidden node = invisible to the throw-only instrument. Fix = `deploy_ensureDeployPanelVisible()` at `deploy_showDetail`/`deploy_showRackDetail` + a key-gated `nav_restore` dispatch guard · **.224** OPS-CONTENT DEAD-TAP SWEEP — 6 sub-tools (vendorEdp/manifest/BOM×2/burndown/audit) hard-coded legacy `#ops-content` (dead under rd); repointed each to its VERIFIED correct host (sub-pane vs `deploy_opsHost()`, not blind), SOP `setTimeout` gated `!rd` · **.225** COMMAND LENS tap-throughs (RACKS→`rd_openOpsTool('rackmap')` floor, BLOCKERS→issues empty-safe, DEPLOYS→deploy list; re-home don't rebuild + `.cc-tap` press) · **.226** build badge.
  - **⚠️ BATCH-VERIFY OWED (at the 6-cap — verify before more ships):** `.220` 3D parity · `.221` SYS→ERRORS · `.222` Forge aisle (SYS→FORGE 3D) · `.224` ops-content sweep · `.225` Command Lens · `.226` badge. (`.223` device-verified.) The `.226` badge makes this pass self-checking (shows the running build).
  - **QUEUE:** Forge Stage 2 (data-driven hero + spotlight) — GATED on Stage-1 device-verify AND the still-missing `MOCKUP-HYBRID-v13.html` (Stage-4 features mock, not in Downloads; owner to drop). Adversarial reviews across the arc caught+fixed real pre-ship defects (3D RAF/orphan-context leaks; .221 legacy-banner strictness; .223 nav_restore sibling holes; .224 wrong-host family). **Deploy dead-render root cause + the full ops-content family → memory `project_deploy_deadrender_hosts`.**
- Live (prior): `v1.14.217` — **RACK-TEMPLATE LIBRARY (Stage 2a)** (per `HANDOFF-rack-templates.md`). Rack Map floor is platform-aware: `RACK_TEMPLATES` const library (gb300/gb200-nvl72, hgx-b200-dlc, hgx-h200, hgx-h100) + `rackTemplate_keyFor` string→key mapper (gb300/gb200 checked before b200) + `rackViewer_resolvePlatformKey()` (active deploy → derived site-profile platforms → dominant Master platform → null; every hop typeof-guarded → generic rail on miss, never a hardcoded default). `rackViewer_renderFloor(target, platformId)` renders the template zone-band skeleton (kind-tinted bands, height ∝ u×count) when a platform resolves, else the existing generic 24U rail. Additive `.rv-tpl-*` + `.cn-u-{switch|power|cdu|compute|net|manifold|blank}` tints (existing `cn-u-equipment`/`cn-u-context` untouched, Gate 4). **DEVIATIONS:** reused app tokens (--vio/--gold/--cyan/--slate) not the spec's near-dup --violet/--gold; added compute/net/blank tints; floor is a stacked skeleton, not a twin-rail per-U elevation. **SCOPE = 2a; 2b (elevation template fallback + Master `uStart`/`uEnd` ingest validation) folds into the Part A ship** (overlaps its Master merge). Gates green (node ×4+sw, CSS 12 blocks balanced, CRLF, RACK_TEMPLATES zone-sums ≤ frameU, key-mapper order, 3-stamp). **DEVICE-VERIFIED "all good" 2026-07-09.** Prior: **RACK MAP × COMPASS MERGE .216**
- Live (prior): `v1.14.216` — **RACK MAP × CAGE NUT COMPASS MERGE (Stage 1)** (per `HANDOFF-rackmap-compass-merge.md`). Rack Map ops-tool never blank: `rackViewer_renderFloor` paints the Compass twin-rail as the cold-start/gone-rack FLOOR (EDIT 1: floor-first in the open block; a real last-rack restore `innerHTML`-swaps over it — one insertion covers BOTH void cases, no `_rackResolve` edit). EDIT 2: new `rackViewer_renderFloor` from existing `cn-*` classes, decoupled from `#pg-compass`. +2 CSS rules; compass reference door untouched (EDIT 4). **DEVIATION — EDIT 3 (land-on-Elevation) shipped NO-OP:** verified the Rack Map tool always renders via `renderElevation` (no List toggle); `resultView` belongs only to the separate Master DOC surface — flipping its default would change a different tool, unrequested. EDIT 3's intent already satisfied structurally. Gates green (node ×4+sw, CSS 12 balanced, CRLF, `rackViewer_renderFloor`==1, page-independent, 3-stamp). **DEVICE-VERIFIED "all good" 2026-07-09.** Prior: **CRASH-LOG HARDENING .215**
- Live (prior): `v1.14.215` — **CRASH-LOG HARDENING** (per `HANDOFF-v1.14.214-crashlog-hardening.md`; spec targeted `.214` but that stamp was already the deploy-tap fix, so it lands as `.215`). Code-only, 4 edits to the DCT-04 error handler, no asset/markup: (1) `phantomViewCrashLog()` now **copies the full log to clipboard** inside the banner-tap gesture (iOS-gesture-safe) + header + `haptic([8])` + graceful fallback (alert still shows trace if clipboard denied); (2) per-entry `v:` version stamp added to all 3 writers (logCrash + 2 EDP diag), typeof-guarded for the early-boot IIFE; (3) `sameVersion()` auto-clear rewritten to key off `entry.v` (was comparing `src` filenames = always `dct-ios.html` → the stale-entry filter was a **no-op**; now real). Additive schema, clean revert. SW `CACHE_VERSION` bumped (stamp-only, required so devices fetch new bytes). Gates green (node ×4+sw, CSS braces delta 0, CRLF, grep: `v:`=3 / `currentFile`=0, 3-stamp). **DEVICE-VERIFIED "all good" 2026-07-09.** Prior: **FIX deploy scope-picker .214**
- Live (prior): `v1.14.214` — **FIX: deploy scope-picker cab taps eaten (STAGE never enables)** — the real B1 root cause + revert of the `.212` regression. Proven via live DevTools/`elementFromPoint`: `.msc-actions` is `position:sticky;bottom:0`, so it sticks to `#pg-work`'s CONTENT-box bottom = `viewport_bottom − padding_bottom`. `.212` bumped that padding `71px → tabnav-h+96` (~180px), raising the stick-point ~110px UP into the middle of the cab list; the opaque z-index:5 bar floated over the rows and ate their taps → `mscope_toggleCab` never fired → STAGE stayed disabled → "deploy won't work" on phone+laptop. The `transform:none` `.212` added was IRRELEVANT (transform governs `fixed`, not `sticky`) — deleted. FIX: padding back to `calc(var(--tabnav-h)+12px)` so the bar sits just above `#rd-botnav`; foot-pad 112px runway keeps the last cab clear. **Verified live**: reduced padding → tap cab → `.sel` + `msc-confirm-btn` enables. The JS was always correct. ⚠️ INVESTIGATION NOTE: a "page won't scroll" symptom in the automation browser was RULED OUT as an environment artifact (`#pg-cmd` equally unscrollable in that 551px headless window; real devices scroll fine) — NOT an app bug. CSS-only; `?legacy=1` byte-identical. Gates green. **DEVICE-VERIFIED "all good" on phone+laptop 2026-07-09 — deploy scope-picker works.** Prior: **BOOT PLATE SWAP .213** (verified same pass)
- Live (prior): `v1.14.213` — **BOOT PLATE SWAP** (asset-only + required SW bump; per `BUILD-SHEET-2026-07-08.md` RELEASE 2). Portrait boot-hero `plate.webp` upscaled `1024×1536 → 2048×3070` q82 (467KB) — the DPR-3 iPhone landing was soft (diagnosed live). Same filename → zero HTML markup change (only the version constant). **`plate.webp` is precached, so `sw.js` CACHE_VERSION was bumped `.212→.213` — without it devices keep the old 1024 plate (the one gotcha).** `plate-wide.webp` (1672×941) left as-is. srcset idea CANCELLED (owner ruling; single hi-res plate + object-fit:cover). Remote HEAD == mine, no web-Claude collision. Gates green (node ×4+sw, CSS 3716/3716, CRLF uniform, valid VP8, 3-stamp). **Build sheet reconcile: its Releases 1/3/4/5/6 were ALREADY live on my line as `.204/.208/.209/.211/.210`; only Release 2 (this swap) was outstanding — the staged `.203`-based HTML was NOT used (would have regressed .212).** First ship of a NEW batch (`.202-.212` device-verified + released). Prior: **RECONCILE web-Claude .204 superset .212** (files51).
- Live (prior): `v1.14.212` — **RECONCILE web-Claude .204 superset** (files51). My `.204` adopt (9605a9b, files49) was an 11-hunk PARTIAL; files51's `.204` diff is the 14-hunk superset. Three fixes my `.205-.211` line lacked, semantically re-anchored into `.211` (diff was vs web-Claude's `.203` line numbers — applied by meaning): **(1) B1 root fix** — `#pg-work:not(.wk-grid) #wk-deploy:has(.msc-actions){transform:none!important}` nulls the rf-fade identity transform so the sticky scope-picker bar resolves to the viewport (was eating cab-row taps); supersedes my padding-shrink hack (v1.14.116). **(2) last-row clip** — with the root fix, padding relaxes: `#pg-work:has(.msc-actions)` padding-bottom `71px+safe → calc(var(--tabnav-h)+96px)`, `.msc-foot-pad` `88→112px` (dh6:050 was clipping the nav). #1+#2 COUPLED — shipped together. **(3) `.rf-back-btn` chip** (opt-4, fleet-wide, cosmetic, owner-approved) — 56px hit target kept, visible chrome → 34px `::before` chip, slate chevron, cyan on `:active`. Gates green (node ×4, CSS 3716/3716, CRLF uniform, 3-stamp). **Batch `.202-.212` (11 ships) DEVICE-VERIFIED "all good" by owner 2026-07-08 — RELEASED.** Next ship opens a fresh batch. Prior: **FLOOR MAP ACTIVE STRIP .211**
- Live (prior): `v1.14.211` — **FLOOR MAP ACTIVE STRIP** (per-spec `handoff-active-strip-v1.md`). Auto-derived ACTIVE strip ABOVE the floor-map grid in `deploy_renderFloorMap`: horizontal thumb-scan of only the mid-flight racks (`hud_rackFill` state `blocked`/`active`; complete+pending excluded), BLOCKED-first, header `ACTIVE · N RACK(S)` + red `· M BLOCKED`. Refactor (non-negotiable, kills the `.ask` drift class): per-rack tile `<button>` extracted into ONE `deploy_floorTileHtml(deployId, rack, ru, f)` called by BOTH strip and grid; rollups computed in a SINGLE `rollById` pass shared by both — strip adds no second data pass. Feeds forward: pure `deploy_activeRacks(deployId)` (mid-flight id list, row/number sort) left for the future Shift Handoff bundle (helper only, no wiring). Strip gated behind `redesign_isOn()`; `.tile-active-strip`/`.tile-active-hdr` `body.rd`-scoped; `?legacy=1` byte-identical; strip racks ALSO render in their grid row (not deduped, intended). Gates green (node --check ×4 blocks, CSS braces 3712/3712, CRLF uniform, 3-stamp lockstep). **10th ship in open BATCH-VERIFY batch `.202-.211` — well past the 6-cap; owner continuing the stack 2026-07-08.** Prior: **LOGO-HOME .210**
- Live (prior): `v1.14.209` — **LOG NOTE QUICK CHIPS** (per-spec `handoff-lognote-quickchips-v1.md`). Site-editable tap/long-press chips in the rack Log Note prompt (`stripeRack_logNote` via an opts-gated `phantomPromptAsk`). Tap = pre-fill; long-press 500ms = instant `RACK_NOTE` audit + haptic + toast + close (resolves null, no double-log). 12 defaults + site add/edit/remove → `phantom_lognote_chips_v1` (checklist override shape). `phantomPromptAsk` extension non-breaking (other callers unaffected); audit schema unchanged; redesign-only, `?legacy=1` prompt byte-identical. **8th ship in open BATCH-VERIFY batch `.202-.209` — 2 past the 6-cap, owner continued the stack 2026-07-08.** Prior: **PHASE CHECKLIST .208**
- Live (prior): `v1.14.208` — **PHASE CHECKLIST** (per-phase item lists in `deploy_showRackDetail`; per-spec `handoff-phase-checklist-v1.md`). Expandable CHECKLIST strip per phase card: live count meter + bar + accordion rows (green ring-check, per-item notes). Locked defaults + site-level add/edit/remove → `phantom_checklist_site_v1` (first brick of AUS-01 site profile). Per-rack state rides `ph.checks`/`ph.notes` (no new key/migration); rack % untouched; DLC rows gated GB200/GB300/NVL72 fail-open; soft-confirm on COMPLETE w/ unchecked; locked deploys read-only. Redesign-only, `?legacy=1` byte-identical. Deviations (in notes): meter in its own strip header not beside status badge; accordion/edit state in transient module vars. **7th ship in open BATCH-VERIFY batch `.202-.208` — owner stacked past the 6-cap 2026-07-08; verify pending.** Prior: **FIX Command tile .207**
- Live (prior): `v1.14.207` — **FIX: Command deploy tile unclickable on laptop** (owner-reported; root-caused via Chrome automation). #pg-cmd .tile inherited aspect-ratio:1/1.6 from the generic .tile rule (@8415); id-scoped rule never reset it, so on WIDE viewports tiles ballooned to 474px, burying the DEPLOY/SCANNED/HANDOFF row under the fixed bottom nav. Fix: aspect-ratio:auto on #pg-cmd .tile (verified live: 474->96px, tile reachable). Handler was always fine. Prior: **A-2 buttons .206**
- Governing docs: `PHASE0-CENSUS.md` (in repo; LIVE/RE-HOMED/STRANDED/RETIRED verdicts — source of truth for R1 deletion). `PHANTOM-MASTER-REORG-SPEC.md` (roadmap + decision log; Downloads-only — copy into repo if it should govern sessions).
- DONE: Phase-0 census (38 surfaces; awaiting John SIGN-OFF) · R-1 `.169` (Work re-sort, 8 shift cards) · R-2a `.170` (Deploy detail absorbs BURNDOWN/AUDITS via `deploy_detailTool`) · R-2b `.171` (retire the TOOLS grid; ⚙→⚑ BLOCKER) · R-2c `.172` (re-home stranded rack LOG NOTE) · R-3a `.173` (PLATFORMS front door — landing REJECTED, superseded) · `.174` (PARTS label consistency) · **R-3b `.175` (PLATFORMS correction: landing rebuilt in rf-card language via `.deploy-tools-grid`, 6 photoreal platform icons, GB-NVL fold REVERTED to 5 cards, per-platform drill-in `rd_platformOpen` filtering the re-homed Power/LED/Thermal reference cards)**. All Phase-0.5 strands resolved/folded.
- Queue: **Ship B COMPLETE (4/4)** — plan `Downloads/SHIP-B-PLAN.md`: **B1 `.178`** (15 deploy-flow pushes `p:'sop'`→`p:'work'` under rd) · **B2 `.179`** (deleted `deploy_showTools`/`deploy_showOpsTool`; `nav_restore` `'tool:'` repoint changed BOTH condition and call) · **B3 `.180`** (gated `showOpsTab`'s choke-point push) · **B4 `.181` SHIPPED** (central `showPage()`-level guard — whitelist `cmd/work/ref/master`, redirect table honors `o:` via `rd_openOpsTool`, toast on the homeless fallback; HANDOFF flow rd-gated + both renderers → `deploy_opsHost()` [the adversarial-verify BLOCKER: pre-B4 handoff worked *via the pg-sop leak*]; `d==='command'` host fix; rd-gated `d==='mscope'/'mscope-staged'` restore branches with a `showWorkTab('deploy')` drill; `showStab` legacy-`p:` push suppressed under rd). Verified by a 3-lens adversarial Workflow + a delta pass; `?legacy=1` proven byte-identical. → **B4 device-verify (HARD STOP, checklist in version.json)** → **R1 deletion pass** (RETIRED incl. Crash-Cart Mode + dead `rd_openPower`; census sign-off gates it). Known follow-ups (post-B4 data pass, John's call): TRIAGE INDEX entries have no rd home (add `ref:` keys or re-home the flowchart); OPS INDEX entries could carry an rd tool key; ~~back-swipe after a Ref sub-pill flip is a no-op~~ → **B4b `.182`** restored the swipe with an rd-shaped `{p:'ref',g,t}` push + a `nav_restore` `p==='ref'`→`showRefTab` branch (chrome-synced; legacy ternary byte-identical).
- R-3b COMPLETE: `.175` landing rebuilt (rf-card language, 5-card revert, per-platform drill-in) · `.176` hid the duplicate Rack Map pill (Work RACK MAP card is its door) · `.177` re-homed Blast Radius to the Deploy detail via Path A (settable `_br_target` + `OPS_TABS.blast` + Row 2.4 button; pill hidden — Deploy is its sole rd door). `rd_openPower` confirmed dead → R1.
- **P-3 (violet surfaces) — VERIFIED CLOSED 2026-07-09 (owner: "leave as-is").** The CALL 2 law (`dct-ios.html:17310`) reserves violet for (a) DLC-context chips, (b) review/rev states, (c) assistant paste-ticket — all three already render violet in live code (`.mf-pill[data-state="rev"]` `:8073`; A-2 paste ticket; plat drill-in `chipCol` `:17380`). `rd_platChipVio()` has zero consumers **by design** (forward guardrail — no pill carries platform context today). Nothing to apply; do NOT re-open without a NEW (4th) owner assignment.
- ⚠️ Prior queue also listed **R-2.5** and **F-1** — no specs held; John to define or confirm superseded.
- Open decisions on John: **device-verify `.184`+`.185` in one pass (checklists in version.json)** · census SIGN-OFF (gates R1) · R-2.5 / F-1: define or confirm superseded. Icon Batch 2 art arrived via `files (45).zip` (bundled FINAL handoff supersedes the staged spec; §2 pre-shipped as `.183`); `PHANTOM-PUNCH-LIST.md` now EXISTS in repo (created `.185`).
- **Data pass SHIPPED `.184`** (see Live line) — device verify outstanding. Known legacy curiosity (untouched): the TRIAGE entries' `page:'triage'` points at a page their content isn't on (cards live on pg-fiber›fs-triage); legacy behavior preserved as-is, revisit R1-era.
- RETIRED by owner decision: Crash-Cart Mode (do NOT build doors for it; physical deletion waits for R1).

## Communication style
John is terse and field-operational. Match it: lead with status, state deviations plainly, no cheerleading, no hedging. Push back with evidence when the spec or the ask conflicts with live code or these rules — he rewards honest pushback and penalizes churn. When parked, say exactly what you're waiting on and from whom, in one line.
