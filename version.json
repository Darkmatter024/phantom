{
  "version": "phantom-v1.14.162",
  "released": "2026-07-01",
  "prevVersion": "phantom-v1.14.161",
  "notes": "v1.14.162 - Dead-CSS cleanup (housekeeping, zero visible change). Removed two now-dead redesign-only cosmetic rules that styled the hex medallion which has been display:none under body.rd since .156: `body.rd .hdr-shield-img{display:inline-block}` and `body.rd .hdr-ghost-btn:not(.alert){border-color:...}` (+ the orphaned .142 comment that explained them). The hex-medallion RETIREMENT itself was already live since .156 (this is only the leftover dead-rule sweep from the hex-retirement handoff). Untouched: base `.hdr-shield-img{display:none}` (legacy), `body.rd .hdr-ghost-img{display:none}`, the live `body.rd #hdr-overflow-wrap{display:none}` hide, the .hdr-ghost-btn base CSS, the .alert machinery, and identity_labelSync - all legacy-serving. CSS-only, no JS touched. ?legacy=1 byte-identical (ghost button + overflow menu fully live in legacy). phantom-shield-256.webp stays in repo+precache (prune with the deferred asset-housekeeping pass). DEVICE-VERIFY: redesign header still SYS-pill-only, no change vs .161; ?legacy=1 ghost+menu work. ROLLBACK: revert the commit."
}
