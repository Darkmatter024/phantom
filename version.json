{
  "version": "phantom-v1.14.182",
  "released": "2026-07-04",
  "prevVersion": "phantom-v1.14.181",
  "notes": "Master-Reorg Ship B4b — restore the Ref pill back-swipe with an rd-SHAPED nav state (John's call after the B4 trade-off review; stacked on the not-yet-device-verified .181 BY OWNER INSTRUCTION — the device pass covers both). B4 (.181) suppressed showStab's push under rd because its legacy p: ('fiber'/'cli'/'power') violated the no-legacy-p hard rule — at the cost of the back-swipe affordance after a Ref sub-pill flip. B4b restores the affordance without the violation: (1) showStab now pushes {p:'ref', g, t} under redesign (?legacy=1 pushes the exact pre-B4 legacy object — ternary, byte-identical); (2) nav_restore gained an rd-GATED p==='ref' branch that routes through showRefTab({fiber-sub→rf-optics, cli-sub→rf-cli, pwr-sub→rf-hw}[g]) — full chrome sync (bottom nav, mode-ref search bar, _rdMode, grid drop, back title) even when popping from another mode — then the existing g/t replay re-activates the exact pill panel; unknown/missing g falls back to showMode('ref') (grid landing). Legacy restore path is the verbatim pre-B4b code in the else branch. NET BEHAVIOR: one swipe after a pill flip returns to the Ref grid (the pre-B4 muscle memory — the pop hits the seeded root and the .97 stickiness guard); pill→pill Back steps through pills INSIDE the rd frame (pre-B4 that same pop painted the LEGACY page — the leak); deeper excursions restore the exact pill state. No legacy p: ever enters nav state under rd. DEVICE-VERIFY (HARD-STOP, clear SW cache first — covers .181 + .182 together): run the full .181 checklist (handoff doors, TRIAGE-hit toast, 'port map' search → tool, mscope back-restore, no legacy strip anywhere), and REPLACE its item 5 with: (a) Ref›CLI, flip OOB, swipe back → Ref grid; (b) flip OOB then CONSOLE, swipe back → OOB pill active in Ref›CLI (rd frame, no legacy page); (c) flip OOB then CONSOLE, tap COMMAND on the bottom nav, swipe back → lands Ref›CLI with OOB active and the bottom nav / search bar correctly on REF (chrome sync when popping from another mode; NOTE a single flip + mode switch + swipe lands COMMAND by design — that pop consumes the root, not the pill state). Then ?legacy=1: sub-pill flips and Back behave exactly as always. KNOWN COSMETIC (accepted): restoring a Compass pill state titles the header PLATFORMS instead of COMPASS (the g/t replay skips ref_openCompass's title correction) — later pass. ROLLBACK: revert the commit. Gates: 4 script blocks compile, CSS braces 3560 balanced, CRLF preserved, 3-stamp lockstep."
}
