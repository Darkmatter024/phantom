{
  "version": "phantom-v1.14.194",
  "released": "2026-07-06",
  "prevVersion": "phantom-v1.14.193",
  "notes": "SHIP A-2 — Assistant intent flow + premium sheet, kills auto-mic (files47: HANDOFF_A2-intent-flow.md + approved assistant-intent-sheet-mock.html; stacked on unverified .192/.193 by owner drop — one device pass covers .192+.193+.194). ENTRY: Command .ask card onclick openVaSheet(listening)+vaStart -> openVaSheet(intent) — sheet opens COLD, mic untouched; Ref-search voice/ask action rd branch -> intent likewise; pg-triage qa-voice caller is LEGACY-ONLY (rd never renders pg-triage) — UNTOUCHED, byte-identical. STATES: intent (4 machined doors per mock: SPEAK cyan / PASTE TICKET violet / TYPE OR PASTE gold / IMPORT FILE teal — accents are icon-stroke tube glows only, halo OFF; SPEAK subtitle switches to about-the-ticket when chip mounted; NOW— heading variant) · composer (machined mono textarea, ASK PHANTOM commit routes VA.transcript=text -> vaAsk(text) = the exact voice path incl. attached-ticket block + [TRUNCATED AT CAP]; native paste = zero clipboard-permission dependence; MENU back) · listening (entered ONLY from the SPEAK tap -> vaStart() direct-in-gesture; DONE—ASK PHANTOM = recognition.stop() -> existing onend asks; CANCEL = abort -> intent; the Release-button-when-done hint is now legacy-only ternary) · ticket attach/clear now re-renders intent (chip above the menu, evidence on board). A-1 plumbing unchanged: paste/fallback/import/256KB bin/12k fence/stateless VA.ticket/vaClose clear. RESKIN: sheet shell ring+brush(::before, was free)+well+machined grab bar; all CSS body.rd; legacy sheet paints byte-identical. A11y: real buttons w/ aria-labels. DELETED: va_intakeRowHtml/va_refreshIntakeRow (superseded by va_intentHtml; grep-verified zero remaining callers). DEVICE-VERIFY (HARD STOP, covers .192+.193+.194 — prior checklists in git history): tap Ask card -> NO mic indicator in Safari chrome, four machined cards; SPEAK -> mic starts only now; CANCEL -> menu, mic off; TYPE OR PASTE -> native paste works no permission prompt -> ASK PHANTOM -> answer addresses pasted content; MENU back = nothing stale; PASTE TICKET -> chip mounts, menu returns, SPEAK subtitle = about the ticket; composer+chip together -> answer uses BOTH; X clears; IMPORT .txt; clipboard-deny -> textarea fallback; close/reopen stateless; ?legacy=1 va sheet + triage Ask Assistant tile unchanged (old auto-mic behavior INTENTIONALLY preserved there); offline fail-fast unchanged. ROLLBACK: revert commit — stateless. Gates: 4 blocks compile, braces balanced, CRLF, lockstep .193 -> .194."
}
