{
  "version": "phantom-v1.14.200",
  "released": "2026-07-07",
  "prevVersion": "phantom-v1.14.199",
  "notes": "SHIP P-2 — New Device v2 first-run reskin + .mach true-up (approved mock new-device-setup-v2.html, files48; CALL 4 stand-in unnecessary — the missing plate WAS the mock, no image asset). RESKIN: firstRun_show() gains a redesign_isOn() branch rendering the mock verbatim (crumb/gradient h1/lede, Chakra sec labels, .fr-mach wrapper fields w/ brush ::before + focus-within glow, hints, optional floor section) — SAME field IDs (fr-operator/facilityId/facilityName/rackNaming/pdu), SAME #fr-error, SAME firstRun_confirm() storage path (merge-not-overwrite, confirmedAt stamp, storage-full error) — ZERO storage/logic change; legacy branch keeps the old gate VERBATIM (runtime-gated). No-regression contract intact: confirmedAt backfill (siteProfile_load) untouched — existing profiles never see the screen. CONFIRM & ENTER: dual-comet conic ring (@property --angle top-level; inline --angle:0deg fallback -> STATIC comet below iOS16.4, boot-button precedent) + blur echo + touch-following spotlight (--mx/--my, passive listeners) + prefers-reduced-motion kill + :active nudge; plate/spot/b structure per mock. Fonts/tokens consumed from the app (Google-Fonts link + duplicate tokens stripped per spec). .MACH TRUE-UP folded in (CALL 3 CLOSED; deviation from dedicated-ship wording — both consume the mock): well -> pill geometry 130/220 at -70 55%-stop, ring stops .2/.26, focus -> mock bright-cyan ring + well glow insets (2px outer ring dropped), border 2->2.5px; consumers .mach/.rack-viewer-input/.va-ticket-ta. BATCH-VERIFY: fresh profile (cleared localStorage) -> v2 screen (comet ring rotating, spotlight follows touch) -> save minimal (name+site) -> enters app -> SYS>SITE PROFILE shows values -> floor fields editable later -> EXISTING-profile device NEVER sees the screen -> reduced-motion device: ring static -> ?legacy=1 fresh device: OLD gate verbatim -> rack input + ticket textarea: slightly deeper well/brighter focus (true-up). ROLLBACK: revert commit (template branch + CSS + true-up). Gates: 4 blocks compile, braces balanced, CRLF, lockstep .199 -> .200. CITES: CALL 0, CALL 3 (closed), CALL 4 (moot), queue-7."
}
