{
  "version": "phantom-v1.14.319",
  "released": "2026-07-20",
  "prevVersion": "phantom-v1.14.318",
  "notes": "SHIP v1.14.319 - icon refresh, batch 1 of the 32-tile redesign: install 8 of 9 web-Claude icons as drop-in replacements (same filenames, new art). Owner confirmed in chat: OPTICS + RACK MAP take the new art; MASTER FILE KEEPS its current 'MASTER DOC / DEPLOYMENT AUTHORIZED' plaque icon (phantom-tool-masterdoc-256.webp was NOT overwritten - the new checklist/shield art dropped the owner-blessed plaque and read more like AUDITS/KNOW, so it was held). INSTALLED (overwrote /icons/, names unchanged): phantom-ref-optics-256 (QSFP/SFP transceivers + MPO fiber breakout + fiber-light burst), phantom-tool-rackmap-256 (three rack cabinets in elevation), phantom-tool-audits-256, phantom-tool-burndown-256, phantom-ref-cli-256, phantom-ref-hwref-256 (PARTS tile), phantom-ref-compass-256, phantom-plat-shared-256 (binds to the SHARED sub-card inside Platforms - teal 'Derate/3-phase/DLC-CDU', NOT the top-level PLATFORMS tile which keeps phantom-ref-hardware). All 8 verified: valid WebP VP8X w/ alpha, ~256px square, names match existing /icons/ files (git shows 8 modified not untracked), each still referenced in dct-ios.html (no orphans). CACHE MECHANISM: filenames are unchanged, so the ONLY cache-bust for these icons is the version triple - hence a real three-stamp bump (.318->.319) even though it is 'just' an asset swap; on SW install the new CACHE_VERSION re-precaches all icon URLs fresh and activate evicts the old cache. No HTML/CSS/JS logic changed beyond the version const. ?legacy=1 byte-identical (same img src paths). DEVICE-VERIFY (John): load Work + Crash Cart + Platforms>SHARED on phone AND laptop - the 8 new icons render (not the old ones), sharp, no white halo; MASTER FILE still shows the plaque. REMAINING: this is 8 of 32 tiles; the other 24 keep current art until later batches (Work/Crash Cart will be a mix, expected). NEXT: N-12 phase stepper (backlog)."
}
