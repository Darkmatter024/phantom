{
  "version": "phantom-v1.14.156",
  "released": "2026-07-01",
  "prevVersion": "phantom-v1.14.155",
  "notes": "v1.14.156 - Header: SYS pill gets ambient state-glow (state-colored bloom + border, lit SYS label, bottom light-sweep, squared corners) and the medallion overflow trigger is retired in redesign. Its actions (BACKUP, RESTORE, SET IDENTITY, HW REF) relocate into the SYS pill's existing tap-to-open panel below the 4 health rows; SITE PROFILE already lived there, ACTION STRIPE stays legacy-only. One pill now holds everything. All changes body.rd-scoped mirroring the profile-row gate, so ?legacy=1 header is byte-identical (medallion + plain pill retained). hdr_aggToggle behavior unchanged; new rd-only identity label id kept in sync via identity_refreshOverflowLabels. DEVICE-VERIFY (HARD-STOP): v1.14.156; medallion gone + pill glows/sweeps in redesign; SYS panel shows HEALTH + ACTIONS + SITE PROFILE, all fire; identity label updates; ?legacy=1 header unchanged. ROLLBACK: revert the added CSS/markup/2 JS lines + 3 stamps."
}
