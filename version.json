{
  "version": "phantom-v1.14.171",
  "released": "2026-07-03",
  "prevVersion": "phantom-v1.14.170",
  "notes": "Master-Reorg SHIP R-2b — retire the redesign TOOLS grid from live navigation (redesign-only edits, legacy byte-identical, ZERO shared-router touch). Now that every former TOOLS tenant has its own home (BOM/MANIFEST/PORT MAP/RACK MAP → Work cards via rd_openOpsTool; SOPS → KNOW on Crash Cart; BURNDOWN/AUDITS → Deploy detail via deploy_detailTool [.170]), the ⚙ TOOLS grid is redundant. ⚠️ADVERSARIAL VERIFY CAUGHT A BLOCKER before push: the 'Log a Blocker' one-tap had NO other redesign door — its #cmd-blocker-card sibling lives in LEGACY pg-triage (which the redesign never renders), so removing the ⚙ TOOLS grid would have STRANDED it. FIX: (1) REPLACED the ⚙ TOOLS button in deploy_showCommandCenter with a direct ⚑ BLOCKER button (onclick=blocker_quick(), redesign-gated) — same header slot, same button count, and now a ONE-tap door (was 2-tap via the grid). blocker_quick() is context-graceful (resolves the active deploy/rack or prompts pick-deploy/pick-rack/empty), so it works from the Deploy dashboard. This both removes the grid door AND re-homes the last stranded tenant. (2) Re-pointed deploy_showOpsTool's '‹ Back' from the now-dead TOOLS grid → wk_showGrid() (Work grid), sealing the one obscure history-restore path back to the grid and harmonizing with rd_openOpsTool. deploy_showOpsTool is a redesign-only function (NOT the shared router) — no nav_push/nav_restore/showPage change. (3) deploy_showTools/deploy_showOpsTool KEPT as nav-restore shims (typeof-guarded branches at nav_restore ~15866/15868 stay valid for any stale pre-deploy 'tools'/'tool:' history entry). Both edits are redesign-only → ?legacy=1 byte-identical (legacy Deploy uses the #ops-tab-strip/showOpsTab, untouched). DELIBERATELY DEFERRED (with reasons): full deletion of deploy_showTools/deploy_showOpsTool + the nav_restore 'tools'/'tool:' branch cleanup = a SHARED-ROUTER touch → held for the router-gating ship (Ship B) / R1, which needs John's explicit router sign-off (same gate Ship B is parked behind); BLAST RADIUS entry into Deploy detail → R-3 (the PLATFORMS rebuild is its natural home; renderBlastRadius() takes no arg + hard-targets #br-wrap so a true re-host = R4-forbidden internal rewrite, and a cross-mode nav-entry now would strand the back); PHASE WORK (legacy-only surface @~18825) + LOG NOTE→Handoff (rack-detail-armed toolbar @~42102) strand folds → R-2c (each needs its own recon). DEVICE-VERIFY (HARD-STOP, clear SW cache first): Deploy dashboard header shows ⚑ BLOCKER + LIST + ＋ NEW (no ⚙ TOOLS); tapping ⚑ BLOCKER opens the Log-a-Blocker flow; every former tool still reachable — BOM/MANIFEST/PORT MAP/RACK MAP from the Work grid, SOPS from KNOW, BURNDOWN/AUDITS from a deployment detail; ?legacy=1 identical (legacy Deploy still has its #ops-tab-strip). ROLLBACK: revert the commit. NOTE: cork still open — John owes device-verify of .167/.168/.169/.170/.171 (5 stacked, ran on faith per 'R-2b go')."
}
