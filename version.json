{
  "version": "phantom-v1.14.192",
  "released": "2026-07-06",
  "prevVersion": "phantom-v1.14.191",
  "notes": "SHIP G-1c2 — GLASS SKIN v1, JS-inline plate pass via .gsk utility. MECHANISM: inline styles beat class rules, so converted templates get class=gsk whose body.rd ring+well is !important (the ONLY sanctioned !important in the glass system); inline styles left in place = minimal diff, radius stays each plate own value (geometry frozen); .gsk is INERT under ?legacy=1 (no rule outside body.rd) — shared templates render identical pixels in legacy, markup gains one inert attribute (reported, not a behavior change). CONVERTED (10 sites, every one fn-mapped before editing): deploy_renderFloorMap card, deploy_showRackDetail x2 (phase card + mono row), renderOpticScanResult alternate-match card, handoff listSection record card, deploy_showAuditLog entry card, bomRenderResults x4 mono status/section rows. Pattern-verified: 2 blanket replace-alls on exact neutral style-string prefixes (semantic gold/red/green plates + class-carrying gx plates + dynamic-bg plates auto-excluded because their strings differ) + 2 anchored singles. deploy_showTombstone site deliberately SKIPPED (modal = bucket D; it shares pattern 2 — that is why pattern 2 was NOT blanket-replaced). OPS SIGNAL dashed empty plate now a DOCUMENTED EXCLUSION (owner ruling 2026-07-06). REMAINING inline C-sites -> G-1c3: issues list/detail, TODAY KPIs, burndown chart cards, profile editor, bom-ingest steps, edp status, reconcile/preflight, AUDIT form panels, nvidia telemetry, action-stripe chips, handoff generator section, phase-matrix (dynamic bg needs a different mechanism), optic ledger (violet accent ruling needed). DEVICE-VERIFY (HARD STOP): Work>Rack Map floor view empty/cards; rack detail phase card + audit-scan mono row; Handoff log record cards; Deploy audit log entries; BOM analyze result rows; optic scan alternate-match card — all machined; tombstone modal UNCHANGED; spot-check one legacy (?legacy=1) render of rack detail/audit log = identical pixels. ROLLBACK: revert commit (CSS block + 10 class attrs). Gates: 4 blocks compile, braces balanced, CRLF, lockstep .191 -> .192."
}
