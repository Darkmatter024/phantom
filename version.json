{
  "version": "phantom-v1.14.187",
  "released": "2026-07-05",
  "prevVersion": "phantom-v1.14.186",
  "notes": "SHIP N-1 — Master naming sweep (HANDOFF_micro-ships_N1-naming_S1-proxykey.md, files46; pulled AHEAD of blocked P-2 by owner instruction 2026-07-05). Label-only, zero logic: (1) Deploy empty-state SVG AWAITING EDP -> AWAITING MASTER, blinking cursor x 17->32 (parent g is text-anchor:middle — EYEBALL cursor position on device, adjust +/-6 if it sits wrong of the R); (2) rack canvas empty state 'No slot data in EDP' -> 'No slot data in Master'; (3) OpticScore override chip label 'EDP Mismatch' -> 'Vendor Mismatch' — the COMMITTED VALUE 'EDP MISMATCH' is UNCHANGED (stored in existing records; display relabels, storage stays); (4) do-not-rename comment added at DEPLOY_EDP_CACHE_KEY (phantom_edp_cache_v1 keeps its name — field migration cost). Vendor-reconcile 'vendor EDP' copy untouched by design. Internal edp_* names untouched. DEVICE-VERIFY (HARD STOP): Deploy empty state on a cleared/fresh state shows AWAITING MASTER with cursor sitting right of the R; Rack canvas empty state reads Master; OpticScore override sheet shows Vendor Mismatch and committing it still writes 'EDP MISMATCH' (check an existing record still matches); ?legacy=1 sweep — these three surfaces render in BOTH houses (labels changed in legacy too, EXPECTED: naming sweep is fleet vocabulary, not a UI-house change). ROLLBACK: revert commit + three stamps. Gates: 4 script blocks compile, CSS brace balance, CRLF preserved, 3-stamp lockstep .186 -> .187. QUEUE: P-2 New Device Setup reskin BLOCKED on missing new-device-setup-v2.html plate; S-1 proxy key waits on grace-mode Worker deploy (John's Cloudflare)."
}
