{
  "version": "phantom-v1.14.199",
  "released": "2026-07-07",
  "prevVersion": "phantom-v1.14.198",
  "notes": "SHIP S-1 (client half) — proxy shared key (files46 micro-ships handoff; directive: buildable now, INERT until the Worker enforces). PHANTOM_PROXY_KEY const inserted after PHANTOM_PROXY_URL + x-phantom-key header added to the single proxy fetch (both anchors unique-verified at .198). Secret generated at ship time per spec (openssl rand -hex 24); value lives in this public source BY DESIGN (not secrecy — raises drive-by abuse cost alongside origin allowlist + per-IP rate limit + spend cap). SEQUENCING SAFE: the grace-mode Worker serves unkeyed requests, so fielded PWAs keep working in any order; OWNER ACTIONS remaining: (1) paste files46/phantom-worker-hardened.js into Cloudflare w/ ENFORCE_KEY=false + wrangler secret put PHANTOM_KEY = the client value; (2) days later, after Worker logs show ~0 unkeyed PHANTOM requests, flip ENFORCE_KEY=true — no app ship needed. BATCH-VERIFY item: any AI feature round-trips (grace Worker) + Worker log shows the key header arriving. ROLLBACK: revert commit; Worker unaffected. Gates: 4 blocks compile, braces balanced, CRLF, lockstep .198 -> .199. CITES: CALL 0, directive owner-only item 1."
}
