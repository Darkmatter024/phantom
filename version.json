{
  "version": "phantom-v1.14.126",
  "released": "2026-06-28",
  "prevVersion": "phantom-v1.14.125",
  "notes": "v1.14.126 - UI sweep RULE C (cards land on their real surface, no dead interstitials) - Handoff. The Work>Handoff card landed on a '#wk-handoff' stub ('SHIFT HANDOFF' + an 'Open Handoff ->' button) before the actual handoff flow. Unlike Deploy (inline dashboard, fixed in B4), handoff_quick() opens a MODAL overlay - so the fix routes Handoff entries straight to the modal, skipping the stub: the HANDOFF rf-card onclick showWorkTab('handoff')->handoff_quick(), and cmd_route('work','handoff')->handoff_quick() (covers the Command-page Handoff launcher + the NBA/OPS-SIGNAL 'GO TO HANDOFF' routes). The modal opens over the current page; the #wk-handoff stub panel is never activated (so no stub shown before OR after). Browser-verified at 412x915: card tap + cmd_route both open the handoff modal directly; #wk-handoff not stab-active; zero console errors. Scan + Issues cards were already direct (real re-homed surfaces), so Rule C is complete (Deploy=B4, Handoff=this). JS + markup, redesign-only -> ?legacy=1 byte-identical. node --check x3 + sw.js, brace gate, three-stamp lockstep. DEVICE-VERIFY: Work grid -> tap HANDOFF -> handoff flow opens immediately (no 'Open Handoff' middle screen); Command 'Handoff' launcher + 'GO TO HANDOFF' prompts same; ?legacy=1 unchanged."
}
