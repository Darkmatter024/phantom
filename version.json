{
  "version": "phantom-v1.14.169",
  "released": "2026-07-02",
  "prevVersion": "phantom-v1.14.168",
  "notes": "Master-Reorg SHIP R-1 — Work grid re-sorted to shift-shape (markup-only, redesign #work-grid, legacy byte-identical). (1) #work-grid reordered to the 8-card shift order: DEPLOY, MASTER FILE, BOM, MANIFEST, PORT MAP, RACK MAP, SCAN, HANDOFF. (2) Dropped 2 cards from Work: SITE PROFILE (its editor already has a redesign door — the SYS panel 'SITE PROFILE / EDIT →' row #hdr-agg-profile-row → rd_openProfile(), live since v1.14.104; removing the Work card collapses it to ONE door per the reorg's design law, no SYS-menu edit needed since the item already existed) and SOP (duplicate of KNOW, which is the canonical SOP home on Crash Cart). Note: OPS_TABS['sops'] renderer + the Deploy TOOLS SOP tile stay (untouched; SOP still reachable, just not a Work card). (3) Renamed the Crash-Cart/Ref grid card HW REF → PARTS (rf-cname + aria-label; meta 'Cables · Part numbers' unchanged; still onclick showRefTab('rf-hwref')). No route/handler/icon/CSS changes; no new assets. All edits inside redesign-only markup → ?legacy=1 byte-identical. Ship B NOT required for this (router-independent); John chose to run R-1 ahead of Ship B. DEVICE-VERIFY (HARD-STOP, clear SW cache first): Work grid = 8 cards in the order DEPLOY/MASTER FILE/BOM/MANIFEST/PORT MAP/RACK MAP/SCAN/HANDOFF with NO SITE PROFILE and NO SOP cards; SITE PROFILE still reachable + editable via SYS panel → SITE PROFILE (and a save still writes); Crash Cart shows PARTS (was HW REF), opens the same HW-ref matrix; KNOW still opens SOPs; every remaining Work card still opens its tool; ?legacy=1 identical. ROLLBACK: revert the commit. NOTE: cork still open — John owes device-verify of .167/.168 (John authorized R-1 ahead of it; R-1 is router-independent)."
}
