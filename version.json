{
  "version": "phantom-v1.14.324",
  "released": "2026-07-21",
  "prevVersion": "phantom-v1.14.323",
  "notes": "SHIP v1.14.324 - rename the redesign PAGE HEADERS to match the .323 pnav nav (owner in chat: 'rename the page headers too'). Completes the Command/Work/Crash-Cart -> Home/Build/Tools language shift on the VISIBLE surfaces. 12 targeted str_replace, all in redesign pages (#pg-cmd/#pg-work/#pg-ref) so ?legacy=1 stays byte-identical: (1) Command hero eyebrow 'COMMAND LENS' -> 'HOME LENS'. (2) Work: back-title 'Workspace'->'Build', grid glabel 'FIELD WORK'->'BUILD', wk_setBackTitle default + WK_CAT_NAMES fallback 'Workspace'->'Build', back-btn aria 'Back to Workspace grid'->'Back to Build grid'. (3) Crash Cart: back-title 'Crash Cart'->'Tools', grid glabel 'CRASH CART'->'TOOLS', ref_setBackTitle default + REF_CAT_NAMES fallback 'Crash Cart'->'Tools', back-btn aria 'Back to Crash Cart grid'->'Back to Tools grid', search aria 'Filter Crash Cart tools'->'Filter Tools'. LEFT UNTOUCHED (deliberate): the LEGACY #mode-ref tab ('Crash Cart' at L12791, hidden under body.rd - renaming it would break ?legacy byte-identical); all code comments; internal mode keys (showMode still 'command'/'work'/'ref'); page IDs (pg-cmd/pg-work/pg-ref); the console.warn diagnostic string. So the app now READS Home/Build/Tools/Exit end-to-end (nav + page headers) while the CODE keeps command/work/ref internally - a deliberate UI/code naming divergence (noted in memory for future edits). Gates: brace balance UNCHANGED (text-only edits, no structural change), all 4 inline <script> blocks compile, sw.js compiles, triple .323->.324 lockstep, CRLF preserved. ?legacy=1 byte-identical. NOTE requires ?redesign=1 on John's device to escape legacy before verify (see .323). DEVICE-VERIFY (John): Command page eyebrow reads HOME LENS; Build page section reads BUILD; Tools page section reads TOOLS + back-titles match; nothing still says Command/Workspace/Crash Cart in the redesign UI. NEXT: N-12 phase stepper (backlog)."
}
