{
  "version": "phantom-v1.14.145",
  "released": "2026-06-29",
  "prevVersion": "phantom-v1.14.144",
  "notes": "v1.14.145 - BLUE ICON REBRAND Stage 4 (Command launcher row): the 3 #pg-cmd .lbtn launcher tiles (DEPLOY / SCAN / HANDOFF, each with a WORK sublabel) now render the branded blue action art instead of inline cyan line-SVGs - deploy->phantom-action-deploy-upload, scan->phantom-action-scan-radar, handoff->phantom-action-handoff-link (all 3 already precached + already blue from Stage 1, so NO new assets/precache change). Static-markup swap (svg -> img class=lbtn-img) + a new CSS rule #pg-cmd .lbtn-img{width:48px;height:48px;object-fit:contain}; the old #pg-cmd .lbtn svg rule is kept as dead/legacy fallback. Routing unchanged (cmd_route('work',...)). #pg-cmd is REDESIGN-ONLY (hidden under ?legacy=1), so ?legacy=1 byte-identical. Three-stamp lockstep 144->145 (cache bump so the new markup reaches the device). Gates: node --check x3 + sw.js, CSS brace balance (+1 rule), version.json valid JSON, 0 lone-LF. Render-verified iPhone14. DEVICE-VERIFY (gloved): close+reopen, confirm v1.14.145, Command tab - the DEPLOY/SCAN/HANDOFF launcher trio shows the blue photoreal action art (~48px), tapping each still routes to Work>deploy/scan/handoff; airplane reload paints; ?legacy=1 unchanged. This == the parked C4. NEXT (await John, never chain): Stage 5 = SYS-pill EKG->webp -> .146 (CONFIRM w/John - the SYS pill is a LIVE status indicator with data-state checking/ok/warn; swapping to a static img may drop the status coloring/animation; John may take this himself), Stage 6 = bake the locked 0.85x cyan glow on .rf-i-img + .lbtn-img + the nav icon-glow rule -> .147 (supersedes parked C3 glow). BOM tool tile PENDING reroll. PRIOR v1.14.144 = blue Stage 3 (Deploy>Tools 5 tiles + blocker); .143 = shield 42px; .142 = Stage 2 shield+cyan border; .141 = Stage 1 drop-ins."
}
