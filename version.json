{
  "version": "phantom-v1.14.188",
  "released": "2026-07-05",
  "prevVersion": "phantom-v1.14.187",
  "notes": "SHIP F-1 — kill the active-pill halo bleed (HANDOFF_F1-bleed-fix_G1-glass-sweep.md; owner-reported on device at Platforms POWER strip). ONE deletion: the body.rd .subtab-strip .stab.active drop-shadow rule @8889 (pre-skin drill-in glow; filter composites on the capsule alpha silhouette and bleeds past the rim = outer halo, violates locked halo-OFF). Sibling sweep per spec: all 30+ other drop-shadow sites reviewed — wordmark, boot topo/items, header ghost, blueprint icon, SVG diagram strokes, power topo animations, ghost gauge, and the icon-glow system (--icon-glow/rf-card, explicitly out of scope). ZERO other pill/chip/stab drop-shadows exist; .tn-item.active .phantom-icon is legacy bottom-nav (not a skinned family, not rd-scoped) — untouched. Rule was body.rd-scoped so ?legacy=1 is untouched by the removal. DEVICE-VERIFY (HARD STOP, clear SW cache — .187 checklist still outstanding, run both): [.188] Platforms > POWER/LED/THERMAL strip: active pill shows tube + inset spill ONLY, zero light outside the capsule; one Deploy-detail drill-in sub-tab strip: confirm no visual dependents lost; ALSO EYEBALL (report, no code): inactive Platforms-strip pills read as neutral machined housings — if legacy group-tints leak through, REPORT it (own targeted ship, per spec). [.187 if not yet done] AWAITING MASTER cursor position, rack canvas Master, Vendor Mismatch chip. ROLLBACK: restore the single deleted line. NEXT: G-1a full-app surface census (manifest only, NO code) -> owner bucket sign-off (HARD STOP) -> G-1b GLASS SKIN v1 layer(s). Gates: 4 script blocks compile, CSS brace balance, CRLF preserved, 3-stamp lockstep .187 -> .188."
}
