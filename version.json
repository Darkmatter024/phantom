{
  "version": "phantom-v1.14.295",
  "released": "2026-07-19",
  "prevVersion": "phantom-v1.14.294",
  "notes": "SHIP v1.14.295 - CLEAN pass (part 1): removed the dead askrow CSS families orphaned by the .293 askrow-markup retire. NO visual change - every deleted rule matched ZERO elements (verified: 0 class=\"askrow/apill/rpill\" markup, and ls-pill exists only in CSS with no ls-pill element - the LOG pill relocated to the hero verb row in .285). Removed: (A) the two #pg-cmd .apill.ls-pill .av/.ls-ico rules (~L4631); (B) the whole base askrow block - #pg-cmd .askrow, .askrow>*, .apill(+.av/.av img/.ey/h3), .rpill(+b/span) and their .279/.280/reserved comments (~L9167-9186); (C) the desktop @media - dropped .askrow from BOTH shared selector lists (max-width group kept statusrow/lens/stats/nba/trow/sig-* intact; padding group kept statusrow/stats/trow intact) and deleted the dedicated .281 @media .apill row-layout block; (D) the .292 body.rd #pg-cmd .apill de-bloom override (now dead). Surgical selector-trims on the two shared LIVE rules (never deleted the rule). Refreshed the .293 markup tombstone comment to past tense. SCOPE: askrow CSS only - the OTHER CLEAN-window items (R3 BOM tombstones, null-lookup tombstones, and the '_edp_checkpointBootCheck' orphan which CONTRADICTS the ratified R4 'it is a live self-invoked IIFE - do not re-wire' finding) are NOT touched here; they need individual recon before any delete. Three-stamp .294->.295; braces 11755->11737 (removed exactly 18 rule pairs, asserted); CRLF preserved. DEVICE-VERIFY: none needed (pure dead-code delete, no element affected) - but it rides the pending .286->.295 batch sweep. NEXT: recon the remaining CLEAN items (R3 BOM / null-lookup / _edp), then N-09 (FORGE aisle door) + N-06 (Rail Compass) per the EXECUTE-ORDER chain."
}
