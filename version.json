{
  "version": "phantom-v1.14.279",
  "released": "2026-07-17",
  "prevVersion": "phantom-v1.14.278",
  "notes": "SHIP v1.14.279 - COMMAND ASK ROW (markup reorder + new .askrow triplet + assistant asset swap). Per HANDOFF-CMD-ASKROW-279 (files 67.zip: handoff + mockup + asset). Ask stops being a full-width banner and becomes ONE tile in a row of three: .trow (DEPLOY/SCANNED/HANDOFF) moves ABOVE, and a new .askrow replaces the .ask button below it = [Ask] + two non-interactive RESERVED slots. Column order now LENS > stats > NBA > trow > askrow > OPS SIGNAL. CHANGES THE PHONE (not desktop-only like .278). MARKUP: .ask <button> replaced by <div class=askrow> containing <button class=apill> (handler openVaSheet('intent') + aria-label carried over verbatim; <p> and .arw dropped per mock; .prism -> .av, same 56px geometry) + two <div class=rpill aria-hidden=true> (dashed, dimmed, em-dash + RESERVED, NO onclick/role/tabindex - a tappable-does-nothing pill is a No-Silent-Failures violation). ASSET: icons/phantom-ui-assistant-256.webp overwritten with the new portrait bust (184x256 VP8X, 12632 bytes; old was 9324 near-square). img carries height=62 NOT width=62 - portrait art sized by height (near-square takes width). Same filename; sw.js:77 already precaches it; version bump forces re-fetch. CSS: removed the whole body.rd .ask set (Rule F layout + .prism + .prism img + .ey/h3/p/.arw + askBreathe keyframes + its reduced-motion override) AND the GLASS-SKIN body.rd .ask material rule - all orphaned once .ask is gone; added #pg-cmd .askrow/.apill/.rpill (single-line, file style). DEVIATION vs spec (both reported): spec was written against the .278 SPEC not my shipped .278, so it didn't know about (a) my folded-in body.rd .ask{width:100%} in the .278 block - removed here (orphaned) - and (b) askBreathe keyframes+reduced-motion - also removed (leave nothing orphaned). Amended the .278 media block: body.rd .ask -> #pg-cmd .askrow in the cap list; added #pg-cmd .askrow to the padding-reset group (so it caps+zeroes like .trow). LEFT the stale body.rd .ask in the v1.14.164 block (L8749) alone per spec 10 (inert, matches nothing, separate cleanup). VERIFIED LIVE (display path, headless real-layout) - see commit. Gates: node --check clean; CSS braces balanced; CRLF preserved; three-stamp .278->.279. RESERVED-SLOT RULE (owner): .rpill is non-interactive by design; claiming a slot means REPLACING the element, not adding a handler. ART-SIZING RULE (permanent): single-dimension-attr art must match the attr to its aspect - near-square takes width, portrait takes height (2nd time art landed right and rendered wrong). John iPhone gate: order LENS>stats>NBA>DEPLOY/SCANNED/HANDOFF>ASK ROW>OPS SIGNAL; ask row = 3 tiles (Ask + 2 RESERVED); Ask icon = NEW robot bust (black chrome, cyan eye rings), sits inside its plate (height=62 test); tap Ask -> VA sheet on intent; tap both RESERVED -> nothing, no press state; 3 tiles equal w/h at 96px; desktop >=980 all rows still on the 1080 column, three matched bands; hard-reload for new HTML+art."
}
