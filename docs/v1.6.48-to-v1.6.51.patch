# ============================================================
# v1.6.48 -> v1.6.51 source fold-in patch
# Generated 2026-05-28 from commits ef2c8dd (v1.6.48) -> bac3762 (v1.6.51)
# Net effect (v1.6.49 self-reference bug already excluded):
#   * 6 named storage-key constants with string-literal RHS:
#       file scope:  BURNDOWN_KEY, HANDOFF_NOTES_KEY, RACK_VIEWER_LAST_KEY
#       GE IIFE:     GE_LAST_RACK_KEY, GE_LAST_FAULT_KEY
#       AUDIT IIFE:  ACTIVE_AUDIT_KEY
#   * 17 inline-literal call sites swapped for the new constant names
#     (the handoff-notes textarea inline oninput stays as a string literal -
#      inline HTML event handlers cannot resolve lexical const bindings)
#   * Legacy-key reaper IIFE after the BOOT banner clears two orphan keys
#     (phantom_force_lead_v1, gt_lessons) on every boot
#   * Version stamps bumped in lockstep: phantom-v1.6.48 -> phantom-v1.6.51
#     (dct-ios.html PHANTOM_APP_VERSION, sw.js CACHE_VERSION, version.json version)
#
# Apply at the repo root of your source-of-truth:
#   git apply docs/v1.6.48-to-v1.6.51.patch
# Or open in any editor as a reference; format is plain unified diff.
# ============================================================
diff --git a/dct-ios.html b/dct-ios.html
index 4ce3d87..4a97142 100644
--- a/dct-ios.html
+++ b/dct-ios.html
@@ -10012,7 +10012,7 @@ body.has-resume-banner #boot {
 <script>
 // ━━━ PHANTOM Ghost Update — Service Worker registration ━━━
 // Embedded version sentinel — must match sw.js CACHE_VERSION. Bumped together.
-const PHANTOM_APP_VERSION = 'phantom-v1.6.48';
+const PHANTOM_APP_VERSION = 'phantom-v1.6.51';
 // v1.6.31: bind the boot-screen subtitle to PHANTOM_APP_VERSION so
 // future version bumps update it automatically. Prior to this, the
 // subtitle was hardcoded as "CoreWeave · AUS-01 · v1.4.2" and had
@@ -14854,6 +14854,19 @@ function omni_init() {
 }
 
 // ━━━ BOOT ━━━
+// ── LEGACY-KEY REAPER (v1.6.50) ──
+// Production iPads accumulate orphan localStorage keys that were only ever
+// cleared by the dev escape hatch (5-tap-on-logo path). Run once per boot,
+// idempotent (removeItem on a missing key is a no-op). Scope is strict: keys
+// that have no read or write call sites in current source AND were never
+// surfaced to the user. Keys still in active use are NOT touched here.
+(function _phantom_legacyKeyReaper() {
+  try {
+    localStorage.removeItem('phantom_force_lead_v1'); // dev override flag, never read in prod
+    localStorage.removeItem('gt_lessons');            // legacy GT cleanup target, never read
+  } catch (e) { /* localStorage disabled in private mode; ignore */ }
+})();
+
 // ── STEP CARD TAP-TO-FOCUS ──
 document.addEventListener('click', function(e) {
   const step = e.target.closest('.step');
@@ -15485,7 +15498,7 @@ const OPS_TABS = {
       // Silent mode → if the rack is gone (deployment deleted, EDP changed),
       // we land on empty state, NOT a "NOT FOUND" flash. Graceful fallback.
       try {
-        var last = localStorage.getItem('phantom_rack_viewer_last');
+        var last = localStorage.getItem(RACK_VIEWER_LAST_KEY);
         if (last) rackViewer_request(last, true);
       } catch(_){}
       try { rmActive_render('rm-active-pill-mount-ops'); } catch(_){}
@@ -32800,7 +32813,7 @@ function bomPushToApp() {
 
   // Push Burndown Jobs
   if (isEnabled('burndownJobs') && r.burndownJobs && r.burndownJobs.length) {
-    var jobs = safeParse(localStorage.getItem('dct_burndown_v1') || '[]', []);
+    var jobs = safeParse(localStorage.getItem(BURNDOWN_KEY) || '[]', []);
     r.burndownJobs.forEach(function(job) {
       jobs.push({
         id: genId(),
@@ -32812,7 +32825,7 @@ function bomPushToApp() {
         archived: false
       });
     });
-    safeStore('dct_burndown_v1', JSON.stringify(jobs));
+    safeStore(BURNDOWN_KEY, JSON.stringify(jobs));
     pushed.push(r.burndownJobs.length + ' burndown job(s)');
   }
 
@@ -33473,7 +33486,7 @@ function rackViewer_request(rackId, silent) {
   // We persist the human-readable name (rackId/name), not the synthetic .id.
   try {
     var lastKey = (rack.rackId || rack.name || rack.id || '').toString();
-    if (lastKey) safeStore('phantom_rack_viewer_last', lastKey);
+    if (lastKey) safeStore(RACK_VIEWER_LAST_KEY, lastKey);
   } catch(_){}
   if (input) input.value = '';
   // Silent mode (auto-restore on tab entry) suppresses the success toast.
@@ -35603,6 +35616,8 @@ const GE = (() => {
   const DB_NAME    = 'GhostEchoDB';
   const DB_VERSION = 1;
   const STORE      = 'ghosts';
+  const GE_LAST_RACK_KEY  = 'ge_last_rack';
+  const GE_LAST_FAULT_KEY = 'ge_last_fault';
   let   db         = null;
   let   pendingId  = null; // ID of last logged echo awaiting outcome
 
@@ -35843,7 +35858,7 @@ const GE = (() => {
       const sheet = document.getElementById('geSheet');
       if (!sheet) return;
       // Pre-fill rack from last used
-      const lastRack = localStorage.getItem('ge_last_rack') || '';
+      const lastRack = localStorage.getItem(GE_LAST_RACK_KEY) || '';
       const rackInput = document.getElementById('ge-rack-input');
       if (rackInput && lastRack) rackInput.value = lastRack;
       // Show log view if no ghosts, replay if we have some
@@ -35869,8 +35884,8 @@ const GE = (() => {
     showReplay: async function() {
       document.getElementById('ge-view-log').style.display = 'none';
       document.getElementById('ge-view-replay').style.display = '';
-      const rackId    = (localStorage.getItem('ge_last_rack') || '').trim();
-      const faultType = localStorage.getItem('ge_last_fault') || '';
+      const rackId    = (localStorage.getItem(GE_LAST_RACK_KEY) || '').trim();
+      const faultType = localStorage.getItem(GE_LAST_FAULT_KEY) || '';
       const ghosts    = await getAllGhosts().catch(() => []);
       const suggs     = getSuggestions(ghosts, rackId, faultType);
       renderSuggestions(suggs, rackId, faultType);
@@ -35896,8 +35911,8 @@ const GE = (() => {
       pendingId = id;
 
       // Persist last used for pre-fill
-      safeStore('ge_last_rack',  rackId);
-      safeStore('ge_last_fault', faultType);
+      safeStore(GE_LAST_RACK_KEY,  rackId);
+      safeStore(GE_LAST_FAULT_KEY, faultType);
 
       // Reset form
       document.getElementById('ge-fault-select').value  = '';
@@ -35915,7 +35930,7 @@ const GE = (() => {
     },
 
     clearRack: async function() {
-      const rackId = (localStorage.getItem('ge_last_rack') || '').trim().toUpperCase();
+      const rackId = (localStorage.getItem(GE_LAST_RACK_KEY) || '').trim().toUpperCase();
       if (!rackId) return;
       if (!confirm('Clear all ghosts for rack ' + rackId + '?')) return;
       await deleteByRack(rackId).catch(e => {});
@@ -35925,8 +35940,8 @@ const GE = (() => {
     clearAll: async function() {
       if (!confirm('Clear ALL ghost echoes? This cannot be undone.')) return;
       await deleteAll().catch(e => {});
-      localStorage.removeItem('ge_last_rack');
-      localStorage.removeItem('ge_last_fault');
+      localStorage.removeItem(GE_LAST_RACK_KEY);
+      localStorage.removeItem(GE_LAST_FAULT_KEY);
       pendingId = null;
       this.showLog();
     },
@@ -35953,6 +35968,15 @@ function geClearAll()  { GE.clearAll(); }
 
 const OPTIC_INV_KEY = 'phantom_optic_inventory';
 
+// v1.6.49 dedup: pull these three keys out of inline string literals so the
+// data model is greppable from one place. Active read/write keys only; the
+// 6 cleanup-only / dev-only direct-string keys (gt_lessons, phantom_seen_boot,
+// phantom_force_lead_v1, phantom_identity, phantom_rm_last, __phantom_health)
+// stay inline — naming a one-shot removeItem call is cosmetic.
+const BURNDOWN_KEY        = 'dct_burndown_v1';
+const HANDOFF_NOTES_KEY   = 'phantom_handoff_notes';
+const RACK_VIEWER_LAST_KEY = 'phantom_rack_viewer_last';
+
 const _opticStore = DataStore(OPTIC_INV_KEY);
 function loadOpticInventory() { return _opticStore.load(); }
 function saveOpticInventory(inv) { _opticStore.save(inv); }
@@ -36411,7 +36435,7 @@ function autoIdentifyOptic(scanned) {
 // BURNDOWN ENGINE
 // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 const BD = (() => {
-  const KEY      = 'dct_burndown_v1';
+  const KEY      = BURNDOWN_KEY;
   const STATUSES = ['pending', 'progress', 'done'];
   const STATUS_LABELS = { pending: 'PENDING', progress: 'IN PROGRESS', done: 'DONE' };
   const STATUS_ICONS  = { pending: '○', progress: '◑', done: '✓' };
@@ -37337,6 +37361,7 @@ document.addEventListener('visibilitychange', () => {
 // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 const AUDIT = (() => {
   const KEY = 'phantom_audits_v1';
+  const ACTIVE_AUDIT_KEY = 'phantom_active_audit';
   const OPTIC_TYPES = ['SR4','LR4','DR4','ER4','ZR','DAC','AOC','QSFP28','QSFP-DD','OSFP','SFP+','SFP28','OTHER'];
   let currentAuditId = null;
 
@@ -37400,7 +37425,7 @@ const AUDIT = (() => {
     const audit = getAudit(id);
     if (!audit) return;
     currentAuditId = id;
-    safeStore('phantom_active_audit', id);
+    safeStore(ACTIVE_AUDIT_KEY, id);
 
     const c = document.getElementById('ops-content');
     if (!c) return;
@@ -37604,7 +37629,7 @@ const AUDIT = (() => {
     const audits = load().filter(a => a.id !== currentAuditId);
     save(audits);
     currentAuditId = null;
-    localStorage.removeItem('phantom_active_audit');
+    localStorage.removeItem(ACTIVE_AUDIT_KEY);
     showOpsTab('audits');
   }
 
@@ -37909,7 +37934,7 @@ const AUDIT = (() => {
     removeEntry, deleteAudit, pickType, aiIdentify, importFile, exportCSV,
     rapidScan, rapidSubmit, rapidClose, rapidCamera, rapidUndo,
     setPri: () => {},
-    getCurrentId: () => currentAuditId || localStorage.getItem('phantom_active_audit'),
+    getCurrentId: () => currentAuditId || localStorage.getItem(ACTIVE_AUDIT_KEY),
     OPTIC_TYPES,
   };
 })();
@@ -37936,12 +37961,12 @@ function phantomHandoffOpenLegacy() {
 
   // Restore notes
   const notes = document.getElementById('handoff-notes');
-  if (notes) notes.value = localStorage.getItem('phantom_handoff_notes') || '';
+  if (notes) notes.value = localStorage.getItem(HANDOFF_NOTES_KEY) || '';
 
   // Open burndown jobs
   const bdEl = document.getElementById('handoff-burndown');
   if (bdEl) {
-    const jobs = safeParse(localStorage.getItem('dct_burndown_v1') || '[]', []).filter(j => !j.archived);
+    const jobs = safeParse(localStorage.getItem(BURNDOWN_KEY) || '[]', []).filter(j => !j.archived);
     if (!jobs.length) {
       bdEl.innerHTML = '<div style="font-family:var(--raj);font-size:12px;color:var(--slate);padding:8px 0">No open jobs.</div>';
     } else {
@@ -37991,7 +38016,7 @@ function phantomHandoffClose() {
 
 function phantomHandoffExport() {
   const notes = document.getElementById('handoff-notes')?.value || '';
-  const jobs = safeParse(localStorage.getItem('dct_burndown_v1') || '[]', []).filter(j => !j.archived);
+  const jobs = safeParse(localStorage.getItem(BURNDOWN_KEY) || '[]', []).filter(j => !j.archived);
   // Pull from Ghost Echo's actual store. Outgoing layer, newest-first, top 10.
   const _geCache = (typeof ge_load === 'function') ? ge_load() : { outgoing: [] };
   const ghost = (_geCache.outgoing || [])
@@ -38525,7 +38550,7 @@ function exportAllData() {
   if (window.phantomMarkBackupDone) phantomMarkBackupDone();
   const sops = loadSOPs();
   const racks = safeParse(localStorage.getItem('dct_racks_v1') || '[]', []);
-  const burndown = safeParse(localStorage.getItem('dct_burndown_v1') || '[]', []);
+  const burndown = safeParse(localStorage.getItem(BURNDOWN_KEY) || '[]', []);
 
   const dbName = 'GhostEchoDB';
   const storeName = 'incidents';
@@ -39022,7 +39047,7 @@ function phantomImport() {
 
         if (Array.isArray(bundle.sops))     safeStore('dct_sops_v1', JSON.stringify(bundle.sops));
         if (Array.isArray(bundle.rackMaps)) safeStore('dct_racks_v1',    JSON.stringify(bundle.rackMaps));
-        if (Array.isArray(bundle.burndown)) safeStore('dct_burndown_v1', JSON.stringify(bundle.burndown));
+        if (Array.isArray(bundle.burndown)) safeStore(BURNDOWN_KEY, JSON.stringify(bundle.burndown));
         if (bundle.rackHistory && typeof bundle.rackHistory === 'object') safeStore('phantom_rack_history', JSON.stringify(bundle.rackHistory));
         if (Array.isArray(bundle.opticInventory)) safeStore('phantom_optic_inventory', JSON.stringify(bundle.opticInventory));
         if (Array.isArray(bundle.deployments)) safeStore('phantom_deployments_v1', JSON.stringify(bundle.deployments));
diff --git a/sw.js b/sw.js
index ca11d4b..d5c1806 100644
--- a/sw.js
+++ b/sw.js
@@ -27,7 +27,7 @@
 //   Only same-origin assets are cached.
 // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 
-const CACHE_VERSION = 'phantom-v1.6.48';
+const CACHE_VERSION = 'phantom-v1.6.51';
 
 // Assets to precache on install. Keep this minimal — single-file PWA means
 // most of PHANTOM is in dct-ios.html itself.
diff --git a/version.json b/version.json
index a551dc2..ac5dc9d 100644
--- a/version.json
+++ b/version.json
@@ -1,6 +1,6 @@
 {
-  "version": "phantom-v1.6.48",
+  "version": "phantom-v1.6.51",
   "released": "2026-05-28",
-  "prevVersion": "phantom-v1.6.47",
-  "notes": "Phase 2 de-bloat (Search & Destroy) — dead-code + scope removal, no feature changes. (1) Removed orphaned cosmic-* button CSS/JS (.cosmic-wrap/.cosmic-canvas/.cosmic-border-glow/.cosmic-btn, CosmicButton class, initCosmicButtons, and the _cosmicObserver MutationObserver that was re-scanning the entire DOM subtree on every mutation for elements that no longer exist) — all dead since the v1.6.43 ENTER-circle swap. @keyframes cosmic-rotate and @property --border-angle are preserved (still used by the live ENTER circle). Removed the dead BOOT TOPOLOGIES block (bootRenderTopologies + bootTopoOpen + auto-render IIFE) that updated topology tiles removed in v1.6.43. Removed the orphaned OPS_TABS.site delegate (no call sites after v1.6.46) and its two dead .stab[data-tab=site] CSS rules. (2) Fixed two broken routes: showPage('ops',...) -> showPage('sop',...) in the omni-search rack-name and SOP-title handlers (pg-ops never existed). (3) Utility dedup: collapsed three duplicate HTML escapers (_bp_escape, _bomIngest_esc, _bomLookup_esc) into thin delegators over the single escHtml implementation, each preserving its exact null-handling contract (the two BOM escapers now also escape apostrophes -> &#39;, which renders identically); collapsed deploy_formatDate (byte-identical) into formatDate across 9 call sites. (4) Renamed the misleadingly-named phantom_testParseMaster -> master_loadFromPicker (it is load-bearing in the real master-load flow, not a test harness). (5) Scope cut: removed the Arista EOS, Cisco NX-OS, and Mellanox switch-CLI subtabs from pg-cli (general network engineering, out of rack-and-stack scope); retained Console, OOB, Test & Cert, Parser, and IB/NDR. Realigned the cli-sub default to cs-ib, the panel-id registry, and the omni-search nth-child selectors for OOB/Console. Net: 485 lines removed. Verification: JS parse OK (node --check), CSS braces balanced 2352/2352, HTML div nesting preserved."
+  "prevVersion": "phantom-v1.6.50",
+  "notes": "HOTFIX for v1.6.49 regression. The v1.6.49 named-constant refactor used replace_all on quoted string literals to swap inline keys for named constants, which ALSO matched the literals on the right-hand side of my newly-added const declarations. Result: six self-referencing const declarations (e.g. `const BURNDOWN_KEY = BURNDOWN_KEY;`). JavaScript TDZ rule: reading a const inside its own initializer throws ReferenceError. Since the first self-reference was at file scope (BURNDOWN_KEY block), execution halted there and EVERYTHING declared afterward never ran — BD IIFE, AUDIT IIFE, OPS_TABS late additions, boot-restore IIFE, post-boot UI wiring. App painted but no event handlers attached, so all buttons appeared locked with no visual feedback. node --check passed because self-reference is a RUNTIME error, not a syntax error. Fix: restored the six declarations to their proper string-literal initializers — BURNDOWN_KEY, HANDOFF_NOTES_KEY, RACK_VIEWER_LAST_KEY (file scope), GE_LAST_RACK_KEY + GE_LAST_FAULT_KEY (GE IIFE), ACTIVE_AUDIT_KEY (AUDIT IIFE). Also reverted the HTML inline oninput at the handoff-notes textarea back to the literal 'phantom_handoff_notes' — inline event handlers in browsers cannot resolve script-top-level const bindings (they only see globals on window). The const value is still used by the JS-side read site. Builds on v1.6.50."
 }
